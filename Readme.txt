FOR P4Merge.sh

Achtung!
Before running the script:

Either
Set your workspace in Perforce to default workspace with p4 set P4CLIENT=[workspace_name]

Or
Do changes in the script to define your workspace before every Perforce command

